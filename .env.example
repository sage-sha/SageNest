# shared with the GitHub webhook config, verifies X-Hub-Signature-256.
# if left blank, signature checking is skipped entirely.
WEBHOOK_SECRET=

# reserved for future use - not read by any code yet.
# would be needed to authenticate cloning a private repo.
REPO_URL=
GITHUB_TOKEN=
