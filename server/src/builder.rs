use std::path::{Path, PathBuf};

use anyhow::Result;

pub fn clone_repo(clone_url: &str, branch: &str) -> Result<PathBuf> {
    todo!()
}

pub fn build_image(checkout: &Path, commit: &str) -> Result<String> {
    todo!()
}
